package org.liky.game.test;

import org.liky.game.frame.FiveChess;
import org.liky.game.frame.MyChessFrame;

import javax.swing.*;
import java.awt.*;

public class test {
    public static void main(String[] args){
        FiveChess fc = new FiveChess();
        /*
       //JOptionPane.showMessageDialog(mf, "我的信息");//显示一个消息对话框，用来提示信息
        int result = JOptionPane.showConfirmDialog(mf, "我的确认信息，现在开始游戏吗？");//确认对话框，一般有三个选项，是，否，取消
        //为int型数据类型0（是）、1（否）、2（取消）
        if(result == 0){
            JOptionPane.showMessageDialog(mf, "游戏开始");
        }
        if(result == 1){
            JOptionPane.showMessageDialog(mf, "游戏结束");
        }
        if(result == 2){
            JOptionPane.showMessageDialog(mf, "请重新选择");
        }
        String username = JOptionPane.showInputDialog("请输入你的姓名: ");//显示信息输入对话框，作用用来保存用户输入的信息，String类型,
        if(username != null){
            JOptionPane.showMessageDialog(mf,"输入的姓名为:" + username);
        }else{
            JOptionPane.showMessageDialog(mf,"请重新输入你的姓名: ");
        }*/
    }

}

